using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace TowerOfHanoi
{
    /// <summary>
    /// A scriptable object version of an enum which does not require code to add new enum elements,
    /// This state enum is currently being used to define the currently held disk's disk states
    /// </summary>
    [CreateAssetMenu(fileName = "New ScriptableObject Enum", menuName = "SO/New ScriptableObject Enum")]
    public class SO_State : ScriptableObject
    {
        
    }
}